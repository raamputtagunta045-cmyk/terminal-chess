[pytest]
# Put the repository root on sys.path explicitly, so suites under tests/ can
# import chess_game and conftest. This otherwise happens only as a side effect
# of conftest.py sitting at the root, which is easy to break by accident.
pythonpath = .

markers =
    slow: deep perft runs; deselect with -m "not slow"
